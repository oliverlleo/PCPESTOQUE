/**
 * filtros-fix.js
 * 
 * Correção para os filtros de cliente e lista na página de recebimento
 * Este script corrige o problema de filtragem considerando a diferença entre
 * o que aparece na interface (ex: "PVC") e o que está no Firebase (ex: "LPVC")/**
 * filtros-fix.js
 * 
 * A lógica conflitante de sobrescrita da função aplicarFiltros foi removida deste arquivo.
 * A funcionalidade de filtro está agora centralizada em js/recebimento.js.
 */

document.addEventListener('DOMContentLoaded', function() {
    console.log('filtros-fix.js carregado, mas a lógica de sobrescrita de aplicarFiltros foi removida.');
});